import { getLogger } from "../../../log";

const logger = getLogger();

export const command = "show";
export const desc = "shows the current configuration";
export const builder = () => {};
export const handler = () => {};
